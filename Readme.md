 string[] names = ["asd", "wer", "kommer ikke med"];
        // trenger ikke og har string pollinate i ( ) kan gjøre det foran det du vill printe ut exempel under.
        string firstTwoNames = $"{names[0]}, {names[1]}, {names[2]}";
        Console.WriteLine(firstTwoNames);
        // text kan skrives med 3 bunny ears """ fskjdf """ ; LiteclearralText kan bruke spesial tegn i denne typen.
        string text = """sfjsdfjsdfksdfkfhjkdjkhfjksfhskf""";
        Console.WriteLine(text);
        // string builder er en kjappere måte for loops for programmene så er prefered.
        var banan = new StringBuilder();
        // foreac loop under denne.
